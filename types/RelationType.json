{
    "description": "Used to define a relationship with another resource. Examples include the person who manages a holding, or a controlling organisation.",
  
    "type": "object",
  
    "discriminator": {
      "propertyName": "relationType"
    },
  
    "properties": {
      "relationship": {
        "type": "string",
        "description": "How the referenced resource relates to the current resource. Use well-known values where possible."
      },
      "type": {
        "type": "string",
        "description": "The type of the referenced entity (e.g. Person, Organization, Place)"
      },
      "uri": {
        "type": "string",
        "format": "uri-reference",
        "description": "A URL that may be used to retrieve the related resource."
      }
    }
  }